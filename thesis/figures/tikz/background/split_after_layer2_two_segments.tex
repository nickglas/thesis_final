\begin{tikzpicture}[
    font=\normalsize,
    block/.style={
        draw=black!55,
        fill=black!5,
        rounded corners=3pt,
        minimum width=1.35cm,
        minimum height=1.0cm,
        align=center,
        thick
    },
    stem/.style={
        draw=orange!75!black,
        fill=orange!15,
        rounded corners=3pt,
        minimum width=1.35cm,
        minimum height=1.0cm,
        align=center,
        thick
    },
    layer/.style={
        draw=blue!65!black,
        fill=blue!14,
        rounded corners=3pt,
        minimum width=1.35cm,
        minimum height=1.0cm,
        align=center,
        thick
    },
    head/.style={
        draw=green!45!black,
        fill=green!12,
        rounded corners=3pt,
        minimum width=1.65cm,
        minimum height=1.0cm,
        align=center,
        thick
    },
    arrow/.style={
        -{Latex[length=2mm]},
        thick,
        draw=black!70
    },
    split/.style={
        dashed,
        very thick,
        draw=red!75!black
    },
    segmentA/.style={
        draw=orange!75!black,
        fill=orange!7,
        rounded corners=5pt,
        thick,
        inner sep=8pt
    },
    segmentB/.style={
        draw=blue!65!black,
        fill=blue!6,
        rounded corners=5pt,
        thick,
        inner sep=8pt
    },
    node distance=0.55cm
]

% Main pipeline
\node[block] (img) {Image};
\node[stem, right=of img] (stem) {Stem};
\node[layer, right=of stem] (l1) {Layer 1};
\node[layer, right=of l1] (l2) {Layer 2};

% Larger gap around the split
\node[layer, right=1.35cm of l2] (l3) {Layer 3};
\node[layer, right=of l3] (l4) {Layer 4};
\node[head, right=of l4] (head) {Classification\\Head};

% Split coordinate halfway between Layer 2 and Layer 3
\coordinate (splitline) at ($(l2.east)!0.5!(l3.west)$);

% Segment backgrounds
\begin{pgfonlayer}{background}
\node[
    segmentA,
    fit=(img)(stem)(l1)(l2),
    label={[font=\normalsize\bfseries, text=orange!75!black]above:Segment 1 / Service A}
] (segA) {};

\node[
    segmentB,
    fit=(l3)(l4)(head),
    label={[font=\normalsize\bfseries, text=blue!65!black]above:Segment 2 / Service B}
] (segB) {};
\end{pgfonlayer}

% Arrows
\draw[arrow] (img) -- (stem);
\draw[arrow] (stem) -- (l1);
\draw[arrow] (l1) -- (l2);

\draw[arrow]
    (l2) -- node[
        midway,
        above=1.05cm of splitline,
        font=\small,
        text=red!75!black
    ] {activation tensor}
    (l3);

\draw[arrow] (l3) -- (l4);
\draw[arrow] (l4) -- (head);

% Split marker after layer2
\draw[split]
    ($(splitline)+(0,1)$) -- ($(splitline)+(0,-1)$);

\node[
    font=\small\bfseries,
    text=red!75!black,
    align=center,
    below=1.05cm of splitline
] {split point\\after layer2};

\end{tikzpicture}%
apiVersion: v1
kind: Service
metadata:
  name: chain-5svc-mtls-svc-1
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '1'
spec:
  type: ClusterIP
  selector:
    app: thesis-inference
    condition: chain_5svc_mtls
    segment-index: '1'
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chain-5svc-mtls-svc-1
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '1'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-inference
      condition: chain_5svc_mtls
      segment-index: '1'
  template:
    metadata:
      labels:
        app: thesis-inference
        experiment: rq21-chain5-mtls
        workload-role: service
        security-condition: mtls
        mesh-enabled: 'true'
        condition: chain_5svc_mtls
        experiment-condition: chain_5svc_mtls
        segment-index: '1'
      annotations:
        sidecar.istio.io/proxyCPU: 100m
        sidecar.istio.io/proxyCPULimit: 500m
        sidecar.istio.io/proxyMemory: 128Mi
        sidecar.istio.io/proxyMemoryLimit: 512Mi
    spec:
      containers:
      - name: inference
        image: thesisrq21acr26.azurecr.io/thesis-inference@sha256:524e8618c61d97f466b647cdc408e836256a94807790ad66321ae724f975a221
        imagePullPolicy: Always
        ports:
        - containerPort: 50051
          name: grpc
        args:
        - --segment_index
        - '0'
        - --host
        - 0.0.0.0
        - --port
        - '50051'
        - --max_message_bytes
        - '16777216'
        - --split_points
        - layer1
        - layer2
        - layer3
        - layer4
        - --next_address
        - chain-5svc-mtls-svc-2.rq21-chain5-mtls.svc.cluster.local:50051
        env:
        - name: PYTORCH_INTRA_OP_THREADS
          value: '1'
        - name: PYTORCH_INTER_OP_THREADS
          value: '1'
        - name: OMP_NUM_THREADS
          value: '1'
        - name: MKL_NUM_THREADS
          value: '1'
        - name: OPENBLAS_NUM_THREADS
          value: '1'
        resources:
          requests:
            cpu: '1'
            memory: 1Gi
          limits:
            cpu: '1'
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 6
      serviceAccountName: rq21-chain5-svc1
      nodeSelector:
        agentpool: rq15pool
        workload: benchmark
      tolerations:
      - key: workload
        operator: Equal
        value: benchmark
        effect: NoSchedule
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                experiment-condition: chain_5svc_mtls
            topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: chain-5svc-mtls-svc-2
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '2'
spec:
  type: ClusterIP
  selector:
    app: thesis-inference
    condition: chain_5svc_mtls
    segment-index: '2'
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chain-5svc-mtls-svc-2
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '2'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-inference
      condition: chain_5svc_mtls
      segment-index: '2'
  template:
    metadata:
      labels:
        app: thesis-inference
        experiment: rq21-chain5-mtls
        workload-role: service
        security-condition: mtls
        mesh-enabled: 'true'
        condition: chain_5svc_mtls
        experiment-condition: chain_5svc_mtls
        segment-index: '2'
      annotations:
        sidecar.istio.io/proxyCPU: 100m
        sidecar.istio.io/proxyCPULimit: 500m
        sidecar.istio.io/proxyMemory: 128Mi
        sidecar.istio.io/proxyMemoryLimit: 512Mi
    spec:
      containers:
      - name: inference
        image: thesisrq21acr26.azurecr.io/thesis-inference@sha256:524e8618c61d97f466b647cdc408e836256a94807790ad66321ae724f975a221
        imagePullPolicy: Always
        ports:
        - containerPort: 50051
          name: grpc
        args:
        - --segment_index
        - '1'
        - --host
        - 0.0.0.0
        - --port
        - '50051'
        - --max_message_bytes
        - '16777216'
        - --split_points
        - layer1
        - layer2
        - layer3
        - layer4
        - --next_address
        - chain-5svc-mtls-svc-3.rq21-chain5-mtls.svc.cluster.local:50051
        env:
        - name: PYTORCH_INTRA_OP_THREADS
          value: '1'
        - name: PYTORCH_INTER_OP_THREADS
          value: '1'
        - name: OMP_NUM_THREADS
          value: '1'
        - name: MKL_NUM_THREADS
          value: '1'
        - name: OPENBLAS_NUM_THREADS
          value: '1'
        resources:
          requests:
            cpu: '1'
            memory: 1Gi
          limits:
            cpu: '1'
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 6
      serviceAccountName: rq21-chain5-svc2
      nodeSelector:
        agentpool: rq15pool
        workload: benchmark
      tolerations:
      - key: workload
        operator: Equal
        value: benchmark
        effect: NoSchedule
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                experiment-condition: chain_5svc_mtls
            topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: chain-5svc-mtls-svc-3
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '3'
spec:
  type: ClusterIP
  selector:
    app: thesis-inference
    condition: chain_5svc_mtls
    segment-index: '3'
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chain-5svc-mtls-svc-3
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '3'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-inference
      condition: chain_5svc_mtls
      segment-index: '3'
  template:
    metadata:
      labels:
        app: thesis-inference
        experiment: rq21-chain5-mtls
        workload-role: service
        security-condition: mtls
        mesh-enabled: 'true'
        condition: chain_5svc_mtls
        experiment-condition: chain_5svc_mtls
        segment-index: '3'
      annotations:
        sidecar.istio.io/proxyCPU: 100m
        sidecar.istio.io/proxyCPULimit: 500m
        sidecar.istio.io/proxyMemory: 128Mi
        sidecar.istio.io/proxyMemoryLimit: 512Mi
    spec:
      containers:
      - name: inference
        image: thesisrq21acr26.azurecr.io/thesis-inference@sha256:524e8618c61d97f466b647cdc408e836256a94807790ad66321ae724f975a221
        imagePullPolicy: Always
        ports:
        - containerPort: 50051
          name: grpc
        args:
        - --segment_index
        - '2'
        - --host
        - 0.0.0.0
        - --port
        - '50051'
        - --max_message_bytes
        - '16777216'
        - --split_points
        - layer1
        - layer2
        - layer3
        - layer4
        - --next_address
        - chain-5svc-mtls-svc-4.rq21-chain5-mtls.svc.cluster.local:50051
        env:
        - name: PYTORCH_INTRA_OP_THREADS
          value: '1'
        - name: PYTORCH_INTER_OP_THREADS
          value: '1'
        - name: OMP_NUM_THREADS
          value: '1'
        - name: MKL_NUM_THREADS
          value: '1'
        - name: OPENBLAS_NUM_THREADS
          value: '1'
        resources:
          requests:
            cpu: '1'
            memory: 1Gi
          limits:
            cpu: '1'
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 6
      serviceAccountName: rq21-chain5-svc3
      nodeSelector:
        agentpool: rq15pool
        workload: benchmark
      tolerations:
      - key: workload
        operator: Equal
        value: benchmark
        effect: NoSchedule
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                experiment-condition: chain_5svc_mtls
            topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: chain-5svc-mtls-svc-4
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '4'
spec:
  type: ClusterIP
  selector:
    app: thesis-inference
    condition: chain_5svc_mtls
    segment-index: '4'
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chain-5svc-mtls-svc-4
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '4'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-inference
      condition: chain_5svc_mtls
      segment-index: '4'
  template:
    metadata:
      labels:
        app: thesis-inference
        experiment: rq21-chain5-mtls
        workload-role: service
        security-condition: mtls
        mesh-enabled: 'true'
        condition: chain_5svc_mtls
        experiment-condition: chain_5svc_mtls
        segment-index: '4'
      annotations:
        sidecar.istio.io/proxyCPU: 100m
        sidecar.istio.io/proxyCPULimit: 500m
        sidecar.istio.io/proxyMemory: 128Mi
        sidecar.istio.io/proxyMemoryLimit: 512Mi
    spec:
      containers:
      - name: inference
        image: thesisrq21acr26.azurecr.io/thesis-inference@sha256:524e8618c61d97f466b647cdc408e836256a94807790ad66321ae724f975a221
        imagePullPolicy: Always
        ports:
        - containerPort: 50051
          name: grpc
        args:
        - --segment_index
        - '3'
        - --host
        - 0.0.0.0
        - --port
        - '50051'
        - --max_message_bytes
        - '16777216'
        - --split_points
        - layer1
        - layer2
        - layer3
        - layer4
        - --next_address
        - chain-5svc-mtls-svc-5.rq21-chain5-mtls.svc.cluster.local:50051
        env:
        - name: PYTORCH_INTRA_OP_THREADS
          value: '1'
        - name: PYTORCH_INTER_OP_THREADS
          value: '1'
        - name: OMP_NUM_THREADS
          value: '1'
        - name: MKL_NUM_THREADS
          value: '1'
        - name: OPENBLAS_NUM_THREADS
          value: '1'
        resources:
          requests:
            cpu: '1'
            memory: 1Gi
          limits:
            cpu: '1'
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 6
      serviceAccountName: rq21-chain5-svc4
      nodeSelector:
        agentpool: rq15pool
        workload: benchmark
      tolerations:
      - key: workload
        operator: Equal
        value: benchmark
        effect: NoSchedule
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                experiment-condition: chain_5svc_mtls
            topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: chain-5svc-mtls-svc-5
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '5'
spec:
  type: ClusterIP
  selector:
    app: thesis-inference
    condition: chain_5svc_mtls
    segment-index: '5'
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chain-5svc-mtls-svc-5
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: service
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    segment-index: '5'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-inference
      condition: chain_5svc_mtls
      segment-index: '5'
  template:
    metadata:
      labels:
        app: thesis-inference
        experiment: rq21-chain5-mtls
        workload-role: service
        security-condition: mtls
        mesh-enabled: 'true'
        condition: chain_5svc_mtls
        experiment-condition: chain_5svc_mtls
        segment-index: '5'
      annotations:
        sidecar.istio.io/proxyCPU: 100m
        sidecar.istio.io/proxyCPULimit: 500m
        sidecar.istio.io/proxyMemory: 128Mi
        sidecar.istio.io/proxyMemoryLimit: 512Mi
    spec:
      containers:
      - name: inference
        image: thesisrq21acr26.azurecr.io/thesis-inference@sha256:524e8618c61d97f466b647cdc408e836256a94807790ad66321ae724f975a221
        imagePullPolicy: Always
        ports:
        - containerPort: 50051
          name: grpc
        args:
        - --segment_index
        - '4'
        - --host
        - 0.0.0.0
        - --port
        - '50051'
        - --max_message_bytes
        - '16777216'
        - --split_points
        - layer1
        - layer2
        - layer3
        - layer4
        env:
        - name: PYTORCH_INTRA_OP_THREADS
          value: '1'
        - name: PYTORCH_INTER_OP_THREADS
          value: '1'
        - name: OMP_NUM_THREADS
          value: '1'
        - name: MKL_NUM_THREADS
          value: '1'
        - name: OPENBLAS_NUM_THREADS
          value: '1'
        resources:
          requests:
            cpu: '1'
            memory: 1Gi
          limits:
            cpu: '1'
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 6
      serviceAccountName: rq21-chain5-svc5
      nodeSelector:
        agentpool: rq15pool
        workload: benchmark
      tolerations:
      - key: workload
        operator: Equal
        value: benchmark
        effect: NoSchedule
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                experiment-condition: chain_5svc_mtls
            topologyKey: kubernetes.io/hostname
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
spec:
  mtls:
    mode: PERMISSIVE
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: service2-strict
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '2'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '2'
      workload-role: service
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: service3-strict
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '3'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '3'
      workload-role: service
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: service4-strict
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '4'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '4'
      workload-role: service
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: service5-strict
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '5'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '5'
      workload-role: service
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: service2-service1-only
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '2'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '2'
      workload-role: service
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - cluster.local/ns/rq21-chain5-mtls/sa/rq21-chain5-svc1
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: service3-service2-only
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '3'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '3'
      workload-role: service
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - cluster.local/ns/rq21-chain5-mtls/sa/rq21-chain5-svc2
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: service4-service3-only
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '4'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '4'
      workload-role: service
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - cluster.local/ns/rq21-chain5-mtls/sa/rq21-chain5-svc3
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: service5-service4-only
  namespace: rq21-chain5-mtls
  labels:
    app: thesis-inference
    experiment: rq21-chain5-mtls
    workload-role: mesh-policy
    security-condition: mtls
    mesh-enabled: 'true'
    condition: chain_5svc_mtls
    experiment-condition: chain_5svc_mtls
    segment-index: '5'
spec:
  selector:
    matchLabels:
      condition: chain_5svc_mtls
      segment-index: '5'
      workload-role: service
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - cluster.local/ns/rq21-chain5-mtls/sa/rq21-chain5-svc4
